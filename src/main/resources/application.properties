# Common application properties
spring.application.name=autoprint

# Server Configuration
server.port=${PORT:8080}
server.servlet.context-path=/

# JWT Configuration (Override in production with environment variables)
jwt.secret=${JWT_SECRET:U2VjcmV0S2V5Rm9yU2lnbmluZ0pXVG9rZW5BdXRvcHJpbnQxMjM0NTY3ODk=}
jwt.expiration=${JWT_EXPIRATION:86400}

# Razorpay Configuration (Override in production)
razorpay.key.id=${RAZORPAY_KEY_ID:rzp_test_XOOtBJCnTVxJJC}
razorpay.key.secret=${RAZORPAY_KEY_SECRET:}

# Activate the 'dev' profile by default if no profile is specified
spring.profiles.active=dev
razorpay.key.secret=3nqE1Yp2AqscQj9qWQtZK8vB
razorpay.currency=INR
razorpay.receipt.prefix=order_rcpt_
razorpay.callback.url=/api/payments/callback

# CORS Configuration
cors.allowed-origins=http://localhost:3000,http://localhost:8081,http://localhost:5173,https://auto-print-frontend-eosin.vercel.app,https://auto-print-frontend-eosin.vercel.app/

#H2 Database Configuration 
spring.datasource.url=jdbc:h2:file:./data/autoprintdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.h2.console.enabled=true
spring.jpa.hibernate.ddl-auto = update